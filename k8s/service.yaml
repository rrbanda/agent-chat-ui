apiVersion: v1
kind: Service
metadata:
  name: tech-explorer-ui-service
  labels:
    app: tech-explorer-ui
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: tech-explorer-ui
