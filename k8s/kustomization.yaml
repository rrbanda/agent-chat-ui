apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: tech-explorer-ui
  namespace: techx

resources:
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  - route.yaml
  - pull-secret.yaml

commonLabels:
  app: tech-explorer-ui
  version: v1

namespace: techx

images:
  - name: quay.io/rbrhssa/tech-explorer-ui
    newTag: latest
